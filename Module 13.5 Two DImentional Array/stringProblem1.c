#include<stdio.h>
#include<string.h>
int main()
{
    char word[100];
    fgets(word,sizeof(word),stdin);
    int length = strlen(word)-1;
    char newArray[length];
    for(int i=0;i<length;i++)
    {

    }
}

